"use client";

// In-app realtime notification listener + Web Push registration.
// Fleshed out in Phase 4; stub for now so the layout composes.
export function NotificationListener() {
  return null;
}
